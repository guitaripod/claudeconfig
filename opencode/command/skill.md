---
description: Run a skill by name — loads the skill's instructions and executes the task
---

Load the skill whose name is the first word of "$ARGUMENTS" via the skill tool (available skills are listed in the system prompt as `<available_skills>`). Then follow that skill's workflow end-to-end to complete the rest of "$ARGUMENTS". If no skill matches that name, say which skills you do have and suggest the closest match.
